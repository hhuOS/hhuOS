rm
=====
Power off or reboot the machine.

Usage
-----
```
shutdown [OPTION]...
```

Supported options:
* -r, --reboot: Reboot the machine.
* -h, --help: Show this help message and exit.

If no option is specified, the machine will be powered off.
A reboot will be performed instead if the `--reboot` option is specified.
ctest
=====
An application to test various C standard library functionalities.  
It is not a full-featured testing framework, but rather a simple tool to verify the behavior of certain C library functions.

Usage
-----
```sh
echo [OPTION]...
```

Supported options:
 * -h, --help: Show this help message and exit.